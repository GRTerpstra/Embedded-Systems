from Views.View import View
from tkinter import *

class MainView(View):
    def __init__(self, mainModel, redraw = True):
        super().__init__(mainModel, redraw)

        return

    def getScreen(self):
        super().getScreen()

        return